# FastAPI Backend

## Setup

mkdir venv
python3 -m venv venv/

# Activate environment

source venv/bin/activate

# Install packages

pip intsall -r reuqirements.txt

# Start

cd src/
uvicorn main:app --reload
